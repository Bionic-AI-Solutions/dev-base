apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: dev-base-development
  annotations:
    description: "Development environment configuration for dev-base"

namespace: dev-base

resources:
  - ../../base

patchesStrategicMerge:
  - deployment-patch.yaml
  - configmap-patch.yaml

patchesJson6902:
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: dev-base
    path: deployment-patch.json

images:
  - name: docker4zerocool/dev-base
    newTag: dev-latest

replicas:
  - name: dev-base
    count: 1
  - name: dev-base-frontend
    count: 1

commonLabels:
  environment: development

configMapGenerator:
  - name: dev-base-config
    behavior: merge
    literals:
      - NODE_ENV=development
      - DEBUG=true
      - LOG_LEVEL=debug
      - SENTRY_ENVIRONMENT=development

