apiVersion: v1
kind: Service
metadata:
  name: dev-base-ssh-service
  namespace: dev-base
  labels:
    app: dev-base
spec:
  type: ClusterIP
  ports:
  - port: 22
    targetPort: 22
    protocol: TCP
    name: ssh
  selector:
    app: dev-base
